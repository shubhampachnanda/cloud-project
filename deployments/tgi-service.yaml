apiVersion: v1
kind: Service
metadata:
  name: tgi
  namespace: shared-llm
spec:
  type: NodePort
  selector:
    app: tgi
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080  # ✅ <-- This will be your fixed port!
